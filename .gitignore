# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.env
.venv
venv/
*.db
*.sqlite3

# Coverage
.coverage
.coverage.*
htmlcov/
coverage.xml

# Data files
*.csv

# Browser network captures — large vendored JS bundles (Stripe.js etc.)
# trigger false-positive secret scanner alerts (Stripe's own Google Maps
# referrer-restricted key gets flagged). Keep these debug artefacts local.
backend/network_capture_*.json

# Development spec (local only)
docs/SPEC.md
backend/docs/TEST_STRATEGY.md
backend/docs/SPEC.md
backend/docs/AIRPORT_QUOTE_COMPARISON_SPEC.md
backend/docs/AIRPORT_QUOTE_E2E_SPEC.md
AUTO_ROSTER_SWEEP_HANDOFF.md
PROMO_CODES_HANDOFF.md
REFERRAL_FEATURE_BRIEF.md
BOH_PRICING_STRATEGY_BRIEF.md
BOH_PRICING_STRATEGY_ANALYSIS.md

# Migration scripts (contain sensitive patterns)
backend/migrations/

# Operational / one-off scripts — may contain customer refs, Stripe metadata,
# or scraping tooling we don't advertise. Keep the whole folder out of the repo.
backend/scripts/

# PDX exploratory work (separate sub-project) — schema sketches and
# scratch directory kept local while it's being shaped.
CURBSIDE_SCHEMA_V2.md
PDX_SCHEMA_RECOMMENDATIONS.md
TagPDX/

# Playwright E2E debug artefacts (screenshots + per-test logs)
booking_state_*.png
error_test_*.png
no_payment_btn_*.png
no_stripe_frame_*.png
payment_disabled_*.png
promo_apply_failed_*.png
backend/booking_state_*.png
backend/error_test_*.png
backend/no_payment_btn_*.png
backend/no_stripe_frame_*.png
backend/payment_disabled_*.png
backend/promo_apply_failed_*.png
backend/staging_batches.log
backend/staging_logs/

# Local-only roster analysis artifacts (contain prod-data pulls — never commit)
analysis/
analysis_outputs/

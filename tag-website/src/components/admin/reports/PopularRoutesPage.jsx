import ReportsSectionPage from './ReportsSectionPage'

const PopularRoutesPage = (props) => (
  <ReportsSectionPage
    {...props}
    activeTab="reports"
    reportsSubTab="popular"
  />
)

export default PopularRoutesPage
